lua = "{= env.TESTDIR =}/lua/simple.lua"

.include(duplicate=append,priority=0) "{= env.TESTDIR =}/configs/plugins.conf"

milter_headers {

  use = ["x-virus"];

  routines {
    x-virus {
      symbols = ["SIMPLE_TEST"];
    }
  }

}
