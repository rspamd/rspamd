.include "{= env.TESTDIR =}/configs/lua_test.conf"

worker {
  type = "rspamd_proxy";
  count = 1;
  bind_socket = "{= env.LOCAL_ADDR =}:{= env.PORT_PROXY =}";
  milter = false;
  upstream {
    local {
      self_scan = true;
      default = true;
    }
  }
}
