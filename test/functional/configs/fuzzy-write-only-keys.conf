redis {
  servers = "{= env.REDIS_ADDR =}:{= env.REDIS_PORT =}";
}
lua = "{= env.TESTDIR =}/lua/test_coverage.lua";
lua = "{= env.TESTDIR =}/lua/controller_routes.lua";
options = {
	filters = "fuzzy_check";
	pidfile = "{= env.TMPDIR =}/rspamd.pid";
	control_socket = "{= env.TMPDIR =}/rspamd.sock mode=0600";
	url_tld = "{= env.TESTDIR =}/../lua/unit/test_tld.dat";
	dns {
		retransmits = 10;
		timeout = 2s;
	}
}
logging = {
	type = "file",
	level = "debug"
	filename = "{= env.TMPDIR =}/rspamd.log"
}
metric = {
	name = "default",
	actions = {
		reject = 100500,
	}
	unknown_weight = 1
        symbol {
            weight = 10.0;
            name = "{= env.FLAG1_SYMBOL =}";
        }
        symbol {
            weight = -1.0;
            name = "{= env.FLAG2_SYMBOL =}";
        }
}

worker {
	type = normal
	bind_socket = "{= env.LOCAL_ADDR =}:{= env.PORT_NORMAL =}";
	count = 1
	task_timeout = 60s;
}

worker {
        type = controller
        bind_socket = "{= env.LOCAL_ADDR =}:{= env.PORT_CONTROLLER =}";
        count = 1
        secure_ip = ["{= env.LOCAL_ADDR =}"];
        stats_path = "{= env.TMPDIR =}/stats.ucl";
}

# Encrypted with its own keypair: the write-only rule pings this storage
# with the write key; a ping encrypted with any other key is dropped
worker {
	count = 1;
        backend = "{= env.FUZZY_BACKEND =}";
	bind_socket = "{= env.LOCAL_ADDR =}:{= env.PORT_FUZZY =}";
	type = "fuzzy";
	hashfile = "{= env.TMPDIR =}/fuzzy.db";
	encrypted_only = true;
	keypair {
		privkey = "{= env.KEY_PVT4 =}";
		pubkey = "{= env.KEY_PUB4 =}";
	}
}

fuzzy_check {
	min_bytes = 100;
	timeout = 1s;
	retransmits = 10;

	rule {
	  min_bytes = 0;
	  min_length = 0;
		algorithm = "{= env.FUZZY_ALGORITHM =}";
		mode = "write_only";
		write_servers = "{= env.LOCAL_ADDR =}:{= env.PORT_FUZZY =}";
		write_encryption_key = "{= env.KEY_PUB4 =}";
		symbol = "R_TEST_FUZZY";
		max_score = 10.0;
		mime_types = ["application/*"];
		skip_unknown = true;
		fuzzy_map = {
			R_TEST_FUZZY_DENIED {
				max_score = 10.0;
				flag = {= env.FLAG1_NUMBER =};
			}
			R_TEST_FUZZY_WHITE {
				max_score = 1.0;
				flag = {= env.FLAG2_NUMBER =};
			}
		}
	}
}
